# 供应链中台 · 业务逻辑知识库

> **用途**：一份结构化的业务知识沉淀文档。写新 PRD 时查影响面、了解某个模块的业务规则、追溯上下游依赖，都从这里查，不需要去 CHANGELOG.md 翻版本记录。
>
> **更新规则**：业务规则、状态流转、数据字段、模块间关系发生变更时，**同步更新对应章节**。不要等攒多了再改。
>
> **与 CHANGELOG.md 的关系**：CHANGELOG 记录「什么时候改了什么」，本文档记录「系统现在是怎么运转的」。

---

## 目录

- [一、系统全景](#一系统全景)
- [二、需求预测（demand-forecast）](#二需求预测demand-forecast)
- [三、备货计划（stock-plan）](#三备货计划stock-plan)
- [四、采购计划（purchase-plan）](#四采购计划purchase-plan)
- [五、发货计划（shipment-plan）](#五发货计划shipment-plan)
- [六、采购单（purchase-orders）](#六采购单purchase-orders)
- [七、发货单（shipment-orders）](#七发货单shipment-orders)
- [八、模块间关联矩阵](#八模块间关联矩阵)
- [九、跨模块共享数据](#九跨模块共享数据)
- [十、设计决策记录](#十设计决策记录)

---

## 一、系统全景

### 1.1 业务链路

```
需求预测 → 备货计划 → 采购计划 → 发货计划 → 采购单 + 发货单
  (forecast) (stock)   (purchase)  (shipment)  (purchaseOrder + shipmentOrder)
             计划层                  执行层
```

| 层级 | 页面 | 定位 | 状态数 | 技术栈 |
|---|---|---|---|---|
| 计划层 | 需求预测 | 按业务范围维护 SKU 月度销量预测 | 5 | Vue 3 + Element Plus |
| 计划层 | 备货计划 | 需求驱动，制定备货安排 | 8 | 原生查询区 + Vue 3 + Element Plus |
| 计划层 | 采购计划 | 基于备货计划生成采购方案 | 4 | 原生 JS |
| 计划层 | 发货计划 | 采购完成后的发货安排 | 5 | 原生 JS |
| 执行层 | 采购单 | 采购执行单据，收货、货权调整 | 4 | 原生 JS + Vue3（货权弹窗） |
| 执行层 | 发货单 | 发货执行单据，提货、加工、发货、收货全流程 | 8 | Vue 3 + Element Plus |

### 1.2 核心数据对象

| 对象 | 定义 | 来源页面 | 消费页面 |
|---|---|---|---|
| **备货计划** | 基于需求预测的备货安排 | stock-plan | purchase-plan |
| **采购计划** | 将备货计划转为采购方案，含组合品 BOM 层级 | purchase-plan | purchase-orders, shipment-plan |
| **发货计划** | 采购完成后的物流安排 | shipment-plan | shipment-orders |
| **采购单** | 采购执行单据，对接供应商 | purchase-orders | shipment-orders（通过单号引用） |
| **发货单** | 发货执行单据，管理提货→加工→发货→收货全链路 | shipment-orders | —（终端单据） |
| **组合品 BOM** | 定义组合 SKU 与子 SKU 的 1:N 配比关系 | shipment-orders | shipment-orders（加工时消耗子件库存） |

### 1.3 单号体系

| 单号类型 | 前缀 | 格式 | 所在页面 |
|---|---|---|---|
| 中台采购单号 | PO | PO + YYYYMMDD + 序号 | purchase-orders |
| 易仓采购单号 | ECPO | ECPO + YYYYMMDD + 序号 | purchase-orders |
| 领星采购单号 | LXPO | LXPO + YYYYMMDD + 序号 | purchase-orders |
| 中台发货单号 | DN | DN + YYYYMMDD + 序号 | shipment-orders |
| 易仓发货单号 | EC | EC + YYYYMMDD + 序号 | shipment-orders |
| 发货计划号 | SHP | SHP + YYYYMMDD + 序号 | shipment-plan |
| 备货计划号 | CST/YMX | 前缀 + YYYYMMDD + 序号 | stock-plan |
| 采购计划号 | EBA/JAS/… | 前缀 + YYYYMMDD + 序号 | purchase-plan |
| 提货/发货/收货/加工批次号 | SO | SO + 时间戳后 10 位 | shipment-orders（batches） |
| 货件单号 | FBA | FBA + YYYYMMDD + 序号 | shipment-plan |

---

## 二、需求预测（demand-forecast）

### 2.1 定位
计划层的需求输入。按预测批次、平台、店铺、国家、团队、运营负责人和预测类型维护多 SKU 的月度销量预测，并在确认后生成备货计划。

### 2.2 状态定义与流转

| 状态 | 含义 | 下一状态 |
|---|---|---|
| 草稿 | 新建预测单，尚未提交 | 待确认 / 已作废 |
| 待确认 | 已提交，等待业务确认 | 已确认 / 已作废 |
| 已确认 | 预测通过，生成备货计划 | — |
| 已作废 | 预测单被取消 | 可编辑后重新提交 |

### 2.3 核心业务规则
- 一个预测单可维护多个 SKU，月度预测覆盖 2026/08 至 2027/01。
- 预测单确认后生成关联备货计划单号，需求预测页面可定位查看关联备货计划。
- 支持批量提交、批量作废、导入和导出入口；当前原型使用本地模拟数据。

## 三、备货计划（stock-plan）

### 2.1 定位
供应链计划层的起点。运营人员基于需求预测制定各 SKU 的备货计划，包含未来 3 个月的月度销量预估。经确认后流转到采购计划。

### 2.2 状态定义与流转

| 状态 | 含义 | 下一状态 |
|---|---|---|
| 待计划确认 | 新建备货计划，尚未确认 | 待运营确认 / 已作废 |
| 待运营确认 | 计划已拟定，待运营审核 | 待负责人确认 / 已作废 |
| 待负责人确认 | 运营确认通过，待负责人审批 | 待生成采购计划 / 已作废 |
| 待生成采购计划 | 审批全部通过，可执行 | 待生成发货计划（生成采购后）/ 已作废 |
| 待生成发货计划 | 采购计划已生成 | 已执行 / 已作废 |
| 已执行 | 全链路结束（终态） | — |
| 已作废 | 任意环节作废（终态） | — |

### 2.3 核心业务规则
- **编码查询**：支持 SKU、领星 SKU、SellerSKU、ASIN 四种编码类型切换查询，多值逗号/空格分隔
- **月度销量**：展示 2026/08、09、10 三个月的预计销量，支持环比开关
- **筛选维度**：平台、国家、店铺、团队、创建人均为多选精准匹配；时间范围支持创建/更新/期望交货三种维度

### 2.4 关键操作
| 操作 | 触发方式 | 影响 |
|---|---|---|
| 多条件查询 | 查询按钮 | 综合 14 项筛选条件 |
| 确认 | 底部批量按钮 | 推进当前确认环节 |
| 生成采购计划 | 底部批量按钮 | 跳转 purchase-plan 页面 |
| 生成发货计划 | 底部批量按钮 | 跳转 shipment-plan 页面 |
| 作废 | 底部批量按钮 | 任意状态 → 已作废 |

### 2.5 数据字段概要
计划号、SKU、品名、领星SKU、SellerSKU/ASIN、平台、店铺、国家、团队、月度销量×3、采购单号、计划单号、状态、创建人、备注

---

## 四、采购计划（purchase-plan）

### 3.1 定位
将备货计划转为可执行的采购方案。支持组合产品父子层级展示。确认后流转到采购单和发货计划。

### 3.2 状态定义与流转

| 状态 | 含义 | 下一状态 |
|---|---|---|
| 待确认 | 新建采购计划 | 待采购 / 已作废 |
| 待采购 | 计划已确认，等待采购 | 已完成 / 已作废 |
| 已完成 | 采购执行完毕（终态） | — |
| 已作废 | 被取消（终态） | — |

### 3.3 组合产品层级
- 父项以 `-1` 结尾（如 `GGK20260804-1`），`isGroup=true`
- 子项以 `-1-1`、`-1-2` 结尾，`isChild=true`
- 子项行以灰底 `.child-row` 展示，父项行显示展开箭头 `⌄`

### 3.4 核心业务规则
- **编码查询**：同备货计划，六种编码类型
- **箱数异常提醒**：实际与计划存在差异时（如 `11(10.33)`），该列红色标记
- **测试 SKU 标记**：SKU 含 `123456` 时显示 `♧` 标记

### 3.5 关键操作
| 操作 | 说明 |
|---|---|
| 确认 | 批量推进确认环节 |
| 生成采购单 | 基于采购计划生成采购单（跳转 purchase-orders） |
| 生成发货计划 | 基于采购计划生成发货计划（跳转 shipment-plan） |
| 作废 | 作废选中计划 |

---

## 五、发货计划（shipment-plan）

### 4.1 定位
采购完成后的物流安排。决定货物从哪个仓出发、到哪个目的仓、用什么物流渠道。确认后生成发货单。

### 4.2 状态定义

| 状态 | 含义 |
|---|---|
| 待确认 | 新建发货计划 |
| 待发货 | 计划已确认，待实际发货 |
| 运输中 | 货物在途 |
| 已完成 | 已成功关联发货单，发货计划执行完成（终态） |
| 已作废 | 被取消（终态） |

> 注意：发货计划的「待发货」≠ 发货单的「待发货」。发货计划的「待发货」表示计划已确认但还没开始执行，发货单的「待发货」表示提货/加工已完成、可以发运。

### 4.3 核心业务规则
- **18 个筛选维度**：编码（6种）、计划号、发货仓、目的地仓、目的地仓类型、头程物流商、物流渠道、运输方式、平台、国家、店铺、团队、创建人、时间范围、备注、采购单号、发货单号、货件单号
- **行内编辑**：备注、预计发货日期、预计到货日期可直接在表格中编辑
- 与 shipment-orders 共享字段：发货仓、目的仓、物流渠道、运输方式、平台、国家

#### 发货计划与发货单关联规则
1. **可选范围**：新建发货单的「新增 SKU」仅查询并允许选择状态为「待发货」的发货计划；其他状态的发货计划必须在查询结果中被过滤，不可选择。
2. **关联关系**：一个发货计划只能关联一张发货单；一张发货单可以关联多个发货计划（按所选 SKU 行对应的发货计划建立关联）。
3. **提交回写**：发货单提交并进入「待提货」时，系统将发货单号回写至所有关联发货计划；各发货计划状态从「待发货」流转为「已完成」。此处「已完成」表示发货计划已完成向发货单的执行交接，不代表实物已提货、已发货或已收货。
4. **取消回写**：发货单处于「待提货」时取消发货，所有关联发货计划从「已完成」流转为「已作废」。
5. **操作日志**：发货计划需记录关联和取消两类日志，至少包含操作时间、操作人、操作类型、原状态、新状态和关联发货单号；取消时提供「取消原因」文本输入框，非必填，有填写时一并写入日志。

### 4.4 关键操作
| 操作 | 说明 |
|---|---|
| 确认 | 批量确认发货计划 |
| 生成发货单 | 在 shipment-orders 新建发货单时选择「待发货」状态的发货计划；提交并进入待提货后回写发货单号，发货计划转已完成 |

---

## 六、采购单（purchase-orders）

### 5.1 定位
采购执行单据。管理从供应商采购的全过程：采购下单 → 在途跟踪 → 到货入库 → 收货完成。同时支持货权调整（将货权从一个平台/店铺调拨到另一个）。

### 5.2 状态定义与流转

| 状态 | 含义 | 下一状态 |
|---|---|---|
| 待提交 | 新建采购单，尚未提交审批 | 待审批 |
| 待审批 | 已提交，等待审批 | 待收货（审批通过）/ 待提交（驳回） |
| 待收货 | 审批通过，货物在途等待收货 | 已完成 |
| 已完成 | 全部收货完成（终态） | — |

### 5.3 分组表格结构
每个采购单展示两行：
- **分组行（第一行）**：易仓单号 + 状态 + 跟踪号 + 领星单号 + 采购数 + 在途数 + 到货数 + 采购金额 + 税率 + 供应商
- **明细行（第二行）**：中台单号 + 计划号 + SKU + 采购数 + 在途数 + 到货数 + 待发数 + 收货数量 + 收货备注 + 辅料包材费 + 运费 + 其他费用

两行的 checkbox 联动同步。

### 5.4 货权调整弹窗
- **入口**：顶部「货权调整」按钮 / 底部「加入货权调整」
- **适用范围**：在途数量 > 0 的 SKU；无在途数量的 SKU 不会被搜索出来
- **调入信息优先级**：**调入团队 → 调入平台 → 调入店铺 → SellerSKU/ASIN → 调入数量**（团队为第一优先级，选好团队后平台选项按团队过滤）
- **公共库存**：调入团队选择「公共库存」后，平台/店铺/SellerSKU 列显示「不适用」，不绑定具体平台和店铺
- **步骤**：
  1. 搜索 SKU
     - 可按采购单号查询：下拉选择「采购单号（易仓）/ 采购单号（领星）/ 采购跟踪号」，输入单号后过滤
     - 可按 SKU / SellerSKU / ASIN 批量搜索，换行/逗号分隔，最多 100 个
     - 两个条件可组合使用；多次搜索的结果会追加到同一列表
  2. 在查询结果列表中逐行填写：调入团队 → 调入平台 → 调入店铺 → SellerSKU/ASIN → 调入数量
  3. 通过勾选框选择多行，使用上方批量工具栏统一设置团队/平台/店铺/数量
  4. 二次确认后提交
- **唯一列表列**：勾选 | 来源 SKU | 采购单号 | 品名 | 来源平台/店铺/团队 | 采购数 | 在途数 | 可调整数 | 调入团队 | 调入平台 | 调入店铺 | 调入 SellerSKU/ASIN | 调入数量 | 操作
- **调入团队手动选择**：调入团队改为下拉选择（含「公共库存」选项），公共库存时平台/店铺/SellerSKU 显示「不适用」
- **校验**：
  - 调入团队必填，且不能与来源团队相同
  - 非公共库存：平台必填、店铺必填、SellerSKU/ASIN 必填、数量 1~在途数
  - 公共库存：跳过平台/店铺/SellerSKU 校验，仅校验数量
  - 校验失败用消息提示具体 SKU 及原因
- **级联**：选调入团队后平台选项按团队过滤（公共库存时跳过）；选平台后店铺选项动态变化
- **移除**：查询出来的行可点击「移除」从当前调整列表中删除，不参与提交
- **供应商库存货权调整**：与采购单货权调整逻辑一致，搜索条件增加了「供应商」（多选）和「仓库名称」（多选），表格新增「仓库名称」列

### 5.5 核心业务规则
- **12 维查询**：三套单号体系（易仓/领星/中台）模糊查询、SKU、供应商、计划号、批次号、采购仓库、平台、店铺、团队、收货人、收货时间、创建人
- **日期校验**：开始/结束日期必须同填同空，开始 ≤ 结束
- **默认创建人**：查询时默认选 Admin

---

## 七、发货单（shipment-orders）

### 6.1 定位
供应链执行层的核心单据。管理一张发货单包含多个 SKU、每个 SKU 对应一个发货计划的完整履约流程。是所有原型中业务逻辑最复杂的页面。

### 6.2 状态定义与流转

```
                                    ┌─────────────────────────┐
                                    │        草稿              │
                                    │  (新建未提交/可作废)      │
                                    └───────────┬─────────────┘
                                                │ 提交
                                                ▼
                                    ┌─────────────────────────┐
                                    │       待提货             │
                                    │  (仓库安排提货/可作废)    │
                                    └───────────┬─────────────┘
                                                │ 提货完成
                                    ┌───────────┴─────────────┐
                                    │                         │
                              组合SKU库内库存            库存足够/
                              不足（需加工）？          纯普通SKU
                                    │                         │
                                    ▼                         ▼
                        ┌──────────────────┐     ┌──────────────────┐
                        │     可加工        │     │     待发货        │
                        │ (组合加工补足)     │     │ (待物流发运)      │
                        └────────┬─────────┘     └────────┬─────────┘
                                 │                        │
                        所有组合SKU                        │
                        加工完成                           │
                                 │                        │
                                 └────────┬───────────────┘
                                          │ 发货
                                          ▼
                              ┌──────────────────┐
                              │     待收货        │
                              │  (运输中)         │
                              └────────┬─────────┘
                                       │ 收货
                                  ┌────┴────┐
                                  │         │
                             全部收货    部分收货
                                  │         │
                                  ▼         ▼
                          ┌──────────┐ ┌──────────┐
                          │  已完成   │ │ 部分收货  │
                          │  (终态)   │ │(可继续收货)│
                          └──────────┘ └────┬─────┘
                                            │ 继续收货
                                            ▼
                                      ┌──────────┐
                                      │  已完成   │
                                      └──────────┘

草稿 ────────────► 已作废（终态）
                                        手动完成
待收货 / 部分收货 ──────────────► 已完成（终态）

待提货 ──────────► 已取消（终态，取消发货）
```

### 6.3 可加工状态的流转规则（重要！）

可加工是发货单独有的一个中间态，流程如下：

1. **进入条件**：提货完成后，发货单内含组合 SKU，且按「SKU + 供应商」维度判断该组合 SKU 的库内组合品库存 < 提货数量（库存不足，需补足加工）；若库内组合品库存 ≥ 提货数量，则免加工，直接进入「待发货」，不进可加工状态。
2. **停留规则**：只要发货单内有任何一个组合 SKU 的 `processedQty < pick`（加工数量 < 提货数量），发货单就**保持在可加工状态**
3. **退出条件**：发货单内的**所有**组合 SKU 都满足 `processedQty >= pick` 时，自动转为「待发货」
4. **非组合 SKU 不参与判断**：同一发货单内的普通 SKU 不影响加工状态的流转

例：
- DN20260808004 有 3 个组合 SKU：手环 pick=100（已加工 50）、露营 pick=80（未加工）、折叠椅 pick=120（已加工 120）
- 此时手环差 50、露营差 80，两个都没满足 → 保持可加工
- 加工完手环 50 和露营 80 后 → 三个都满足 → 自动转为待发货

#### 组合 SKU 与非组合 SKU 混单规则
1. 一张发货单允许同时包含组合 SKU 和非组合 SKU。
2. 只要该发货单存在任一组合 SKU 未加工完成，即 `processedQty < pick`，发货单必须停留在「可加工」状态。
3. 非组合 SKU 不参与加工完成判断；即使非组合 SKU 没有加工动作，也不得阻塞「可加工 → 待发货」的状态流转。
4. 当且仅当所有组合 SKU 均满足 `processedQty ≥ pick` 时，发货单自动流转至「待发货」。
5. 加工完成的判断基准为该 SKU 的提货数量，不以计划发货数量或申报数量作为加工完成基准。

### 6.4 核心业务规则

#### 刚性规则（不可违反）
| 规则 | 触发点 | 提示 |
|---|---|---|
| 同一发货单必须同一目的仓 | 新建发货单选择 SKU 时 | 提示文案 |
| 亚马逊相关发货单由领星生成，不在中台手动创建（FBA 专属仓、亚马逊平台/店铺选项置灰禁用） | 新建发货单 / 新增 SKU | 选项置灰 + 提示文案 |
| 加工数量 ≤ 可加工数量 | 加工弹窗提交时 | 报错阻断 |
| 新增 SKU 仅可选择待发货状态的发货计划 | 新建发货单选择 SKU 时 | 非待发货计划不展示、不可选 |
| 一个发货计划仅可关联一张发货单 | 发货单提交时 | 已关联计划不可再次选择 |

#### 发货计划关联粒度、锁定与提交事务
- **关联粒度**：当前一个发货计划只对应一个 SKU，发货计划号是该条发货计划的唯一业务关联标识；一条发货计划只能关联一张发货单。
- **候选范围**：新增 SKU 时，仅查询状态为「待发货」且尚未关联发货单的发货计划；亚马逊平台/店铺的发货计划被过滤，不展示、不可搜索。
- **草稿创建即关联**：保存草稿时即在同一事务内校验并关联发货计划（回写发货单号、计划状态流转「已完成」），草稿阶段计划即被锁定，不可被其他发货单再次关联。
- **编辑释放**：草稿/已作废单据可编辑（移除/新增 SKU），被移除的 SKU 对应发货计划释放回「待发货」，可被其他发货单重新关联。
- **并发冲突**：若另一用户已先关联同一发货计划，保存失败并提示该发货计划已关联的发货单号；用户刷新后重新选择。
- **历史快照**：发货单 SKU 明细须落库保存发货计划号、计划发货数量、发货量（原申报数量口径）、平台、店铺、团队、计划备注等快照字段；后续计划或 SKU 主数据变更不得改写历史单据口径。

#### 发货计划回写规则
- 保存草稿（创建）时即回写发货单号至关联发货计划，发货计划状态「待发货 → 已完成」，并记录关联日志（草稿阶段即锁定计划）。
- 草稿作废、待提货取消：关联发货计划状态「已完成 → 已作废」，并记录日志；作废/取消后计划不可再被其他发货单关联。
- 编辑草稿/已作废单据时移除 SKU：对应发货计划「已完成 → 待发货」（释放回可重新关联）。

#### 取消发货的状态边界与设计理由
| 发货单状态 | 是否允许「取消发货」 | 设计理由 |
|---|---|---|
| 待提货 | 允许 | 尚未发生提货、加工或发货等实物履约动作，取消影响最小；可取消发货单，并将关联发货计划作废。 |
| 可加工 | 不允许 | 已完成提货，且组合 SKU 可能已加工或已消耗子件；直接取消会涉及提货回退、加工品处理、库存回滚和费用处理。 |
| 待发货 | 不允许 | 提货和组合加工已完成，货物通常已备妥待出运；直接取消会造成「已提货但单据取消」的履约、库存和费用链路断裂。 |

- 当前仅保留「待提货」状态的取消发货入口；可加工、待发货状态不展示且不允许调用取消发货。
- 「已作废」仅用于草稿作废；「已取消」仅用于待提货取消发货。两种终态不得编辑、不得重新提交。
- 如后续出现已提货后无法继续发运的业务场景，应单独建设「异常终止 / 撤销发运」流程，不复用普通取消发货。
- 异常终止流程至少需明确：终止原因、货物去向（退回发货仓 / 保留提货仓 / 报损等）、已加工组合品处理、提货费与加工费处理、历史履约记录与操作日志保留，以及关联发货计划的后续状态。

#### 数量调整规则
- **适用状态**：仅「可加工」「待发货」「待收货」「部分收货」允许数量调整；「已完成」「已取消」「已作废」不允许调整。
- **调整类型**：按单据当前可调整数据提供提货数量、发货数量、收货数量三种类型；每次仅调整一种类型。
- **调整列表字段**：SKU、发货计划、计划发货数量、申报数量、提货数量、发货数量、收货数量、调整后数量。
- **上下游校验**：调整后提货数量不得超过计划发货数量，且不得小于已加工数量或已发货数量；调整后发货数量不得超过提货数量，且不得小于已收货数量；调整后收货数量不得超过发货数量。
- **发货与收货一致性**：发货数量调整不得低于已收货数量，防止出现「收货数量 > 发货数量」的数量倒挂和多收差异。若确需下调发货数量，应先将收货数量调整至不高于目标发货数量，再提交发货数量调整；当前不提供联动下调收货数量的操作。
- **审计要求**：调整原因必填，备注选填（选择「其他」时备注必填）；每个变更 SKU 记录调整类型、调整前后数量、调整原因、备注、操作人和时间。
- **调整结果**：提交后必须按调整后的累计提货、发货、收货数量重算差异并自动流转单据状态；数量调整属于数据修正，不新增提货、发货或收货批次，不作为补提、补发或补收入口。

#### 提货校验
- 提货数量 ≤ 计划发货数量；剩余可提数量 = 计划发货数量 - 已提货数量
- 提货数量、剩余可提数量及提货差异均以**计划发货数量**为比较基准，不额外以申报数量限制提货上限。
- 提货仓不能为空
- 同一张发货单允许不同 SKU 分别选择不同提货仓；提货批次需记录每个 SKU 实际提货仓。
- 提货量 < 剩余可提时必须选择差异原因（盘亏/货物损坏/货物丢失/其他）；选择「其他」时必须填写文字备注。
- 允许确认「0 件提货」，但必须填写少提原因；未提/少提数量均视为最终少提，不进入后续补提流程。
- 每个 SKU 的提货为一次性最终确认动作；填写差异原因后即可结束该 SKU 的提货环节。

#### 加工校验
- 可加工判定：按「SKU + 供应商」维度判断库内组合品库存，不足时才进入可加工（详见 6.3）。
- 仅组合 SKU 可加工，普通 SKU 不出现在加工弹窗
- 子 SKU 可用总量 = `在库量 + 在库（待发）量 + 在途／在产量`；加工可用量以该总量计算。
- 可加工数量 = `min(提货数量 - 已加工数量, 各子 SKU 可用总量 ÷ BOM 配比)`；即使子件库存可支撑更多数量，也不得超过组合 SKU 的提货数量。
- 加工清单展示「供应商信息」列（供应商 / 供应商编码 / 供应商料号），组合 SKU 行显示组合品供应商，子件行显示子件供应商；支持按供应商多选筛选（模糊搜索）。
- 加工时同步扣减子 SKU 库存（comboSubStock）
- 加工后生成批次记录到 `batches`（type='加工'）

#### 收货规则
- 收货数量 ≤ 剩余可收数量（发货数量 - 已收货数量）
- 收货后剩余 = 0 → 已完成；> 0 → 部分收货
- **收货方式**：以同步易仓库存自动收货为主（定时拉取易仓收货/上架结果，前提开启库存同步），「刷新」可手动触发拉取，必须幂等；自动收货生成批次与操作日志（来源=自动收货）。
- **手动收货入口隐藏（非删除）**：「收货」按钮始终隐藏，页面与后端能力保留，后续恢复按钮即可；库存同步关闭期间运营暂无法收货（业务方已知悉）。
- **目的仓判定**：后续是否对未接入仓库开放手动收货，按发货单目的仓类型判断——目的仓为平台仓（速卖通/美客多）可开放手动收货；目的仓为已接入自动收货的易仓/海外仓不开放。
- **收发差异**：`收货数量 − 发货数量`，负数 = 少收；正数不允许出现

#### 差异数量定义（一级列表「差异数量」列）
| 字段 | 口径 | 正/负含义 |
|---|---|---|
| 申提差异 | `申报数量 − 提货数量` | `0` = 一致；正数 = 少提；负数 = 提货数量大于申报数量。因提货上限以计划发货数量为准，当计划发货数量大于申报数量时允许出现负数 |
| 提发差异 | `发货数量 − 提货数量` | `0` = 一致；负数 = 少发；正数不允许出现 |
| 收发差异 | `收货数量 − 发货数量` | `0` = 一致；负数 = 少收；正数不允许出现 |
| 申收差异 | `收货数量 − 申报数量` | `0` = 一致；负数 = 少收；正数不允许出现 |

- 4 个差异值在一级列表显示为蓝色可点击，点击弹出该单「差异明细」弹窗（按 SKU 展示申报/提货/发货/收货数量与 4 项差异）
- 弹窗内差异值非 0 显示红色，0 显示灰色
- 4 个字段均有悬浮问号提示（只展示计算公式；申提差异中 `0=一致、正数=少提`，其余差异按各自公式展示，不将差异取绝对值）

#### 发货规则（新增）
- 替换原一键发货为完整弹窗交互
- 发货数量 ≤ 剩余可发数量（提货数量 - 已发货数量）
- 累计发货数量 ≤ 累计提货数量；少发数量必须选择差异原因（货物丢失/货物损坏/物流扣货/其他），选择「其他」时必须填写文字备注。
- 允许确认「0 件发货」，但必须填写少发原因；少发数量视为最终少发，不进入后续补发流程
- 每个 SKU 的发货为一次性最终确认动作；未勾选、0 件发货或少发的 SKU 均必须留存少发原因
- 发货备注可选填（最长 200 字）
- 提交后状态 → 待收货

#### 手动完结规则（新增）
- **适用状态**：待收货 / 部分收货
- **适用场景**：实际货物已到仓但系统未完整扫描匹配，需要手动关闭单据
- **必填字段**：完结原因（货物丢失/货物损坏/业务取消/其他）
- **选填字段**：备注（最长 300 字）
- 手动完结只关闭单据状态，不会把未收数量改记为已收数量；未收数量仍按收发差异保留
- **提交后状态**：直接转为「已完成」（终态）
- **支持批量**：选中多张待收货/部分收货单据后统一完结

### 6.5 组合品 BOM 管理

| 组合 SKU | 子件 | 配比 |
|---|---|---|
| SPU-2003 智能运动手环 | SPU-2003-A 手环主机 + SPU-2003-B 手环表带 | 1:1 |
| SPU-9001 户外露营套装 | SPU-9001-A 露营帐篷 + SPU-9001-B 睡袋 | 1:1 |
| SPU-9002 户外折叠椅套装 | SPU-9002-A 椅架 + SPU-9002-B 椅布 + SPU-9002-C 收纳袋 | 1:1:1 |
| SPU-4001 厨房刀具套装 | SPU-4001-A 主厨刀 + SPU-4001-B 水果刀 + SPU-4001-C 刀座 | 1:1:1 |

子 SKU 库存结构：`{ stock: 在库量, pendingStock: 在库待发量, transitQty: 在途/在产量 }`

### 6.6 加工弹窗设计

| 维度 | 单条组合加工（操作列按钮） | 批量组合加工（顶部按钮） |
|---|---|---|
| 触发 | 行操作列「组合加工」链接 | 工具栏「批量组合加工」按钮 |
| 标题 | `组合加工｜DN20260808001` | `批量组合加工` |
| 范围 | 仅当前发货单的组合 SKU | 所有可加工状态发货单的组合 SKU |
| 发货单号列 | 无 | 有（第一列） |
| 快捷按钮 | 「一键填满」「清空」 | 「一键全部填充」「清空」 |
| 顶部 | 无汇总栏 | 蓝底汇总栏：X 张单 / Y 个 SKU / 提货合计 / 可加工合计 |
| 搜索 | SKU + 供应商（多选）搜索 | SKU + 供应商 + 发货单号搜索 |
| 排序 | — | 按发货单号排序 |

表格列：勾选 | 发货单号(批量) | SKU（含组合标签）| 子件SKU（×配比）| 供应商信息（供应商/供应商编码/供应商料号）| 库存明细（在库量/待发量/在途量/总量）| 提货数量 | 加工进度（已加工/可加工）| 本次加工数量（右侧固定）

### 6.7 批次（batches）数据结构

每次提货/发货/收货/加工都记录一条批次：

```javascript
{
  no: 'SO260808001',        // 批次号
  type: '提货|发货|收货|加工',
  date: '2026-08-08 10:30:00',
  skuCount: '1 个',
  qty: 300,
  warehouse: '华南物流中转仓',
  operator: '运营用户',
  status: '已完成',
  details: [{ sku, plan, qty, ... }]
}
```

加工批次额外包含 `subConsumed` 字段：`['SPU-2003-A ×300', 'SPU-2003-B ×300']`

### 6.8 详情页导航

```
发货单信息 → SKU 明细 → 提货明细 → 加工明细 → 收货明细 → 物流费用 → 轨迹信息
```

加工明细列：加工批次 | 加工时间 | 组合 SKU | 发货单号 | 加工数量 | 消耗子件 | 操作人

### 6.9 其他头程费用分摊弹窗

- **布局**：三段式——分摊批次筛选 → 分摊批次列表（勾选） → 费用信息。
- **筛选维度**：发货单号（中台 / ERP）、团队（多选）、提货批次号、费用周期（日期范围）；费用周期属于分摊批次信息。
- **批次列表**：批次号、费用周期、发货单号、计划号、SKU、品名、团队、提货数量、费用信息；勾选即加入分摊，取消勾选即移除；已选批次实时重算分摊金额。
- **费用信息**：费用类型（提货运费 / 其他出库费 / 加工费（物流加工））、费用金额、分摊规则（按数量 / 按金额）、负责人、附件（非必填）、备注；费用周期不在费用信息区录入。
- **原二级弹窗**（按费用周期添加未分摊提货批次）已合并进主弹窗筛选，减少弹窗层级。
- 分摊规则与计算逻辑沿用现有能力，不重构。

### 6.10 SKU 单位头程成本（新增页面）

- **页面位置**：位于左侧导航「发货单」下方。
- **页面定位**：只读查询页，用于查询 SKU 在发货单履约过程中的单位头程成本；不承担物流费用录入或费用分摊操作。
- **数据粒度**：按「发货单 SKU 明细」一行一条展示，不做 SKU 汇总，也不做一二级列表。
- **成本范围**：仅统计已分摊至该 SKU 的实际头程物流费，不包含清关税费、其他物流费、提货费、加工费等费用。
- **成本结果来源**：单位头程成本直接读取现有头程费用分摊功能已经产出的 SKU 单位成本结果；本页面不重新定义、不重复执行费用分摊或单位成本计算逻辑。
- **数量展示**：实际发货数量作为列表展示字段；实际发货数量为 0 的 SKU 仍进入查询结果，单位头程成本无计算结果时展示为空值/短横线，不展示为 0。
- **展示口径**：暂不展示预估单位成本，仅展示现有分摊结果中的实际单位头程成本；金额统一使用 CNY。
- **基础展示字段顺序**：目的仓、中台发货单号、ERP 发货单号、平台、店铺、SKU、实际发货数量、收货数量、SKU 单位头程成本、运输方式、物流渠道。
- **固定列**：目的仓、中台发货单号、ERP 发货单号、平台、店铺、SKU 固定在左侧，不参与横向滚动；允许将中台/ERP 发货单号、平台/店铺、数量、物流信息等合并为双行信息单元以减少横向滚动。
- **基础关联字段**：目的仓、发货单号、ERP 发货单号、发货计划号、平台、店铺、SKU、实际发货数量、收货数量、单位头程成本、运输方式、物流渠道。
- **筛选范围**：支持 SKU、发货单号、发货计划、目的仓、平台、店铺、运输方式、物流渠道、实际发货日期等查询；不新增成本分摊状态筛选。

---

## 八、模块间关联矩阵

### 7.1 上下游依赖

| 上游 | 下游 | 关联方式 | 关联字段 |
|---|---|---|---|
| 备货计划 | 采购计划 | 页面跳转 + 单号引用 | planNo, purchaseNo |
| 采购计划 | 采购单 | 页面跳转 + 单号引用 | planNo, purchaseNo |
| 采购计划 | 发货计划 | 页面跳转 + 单号引用 | planNo, shipmentNo |
| 发货计划 | 发货单 | 页面跳转 + plan 关联 | plan（发货计划号↔发货单 items[].plan） |
| 采购单 | 发货单 | 单号引用 | 易仓单号 ecNo / 领星单号 lxNo |

### 7.2 改一个字段影响哪些页面

| 改动 | 影响页面 |
|---|---|
| 新增/修改 SKU | stock-plan, purchase-plan, shipment-plan, shipment-orders（skuCandidates） |
| 修改组合品 BOM | shipment-orders（comboBom + comboSubStock） |
| 修改状态流转规则 | 当前页面的 transition / submit 逻辑 |
| 新增筛选维度 | 当前页面的 applyFilters + state + HTML |
| 修改批次数据结构 | shipment-orders 的 submit* 函数 + detailRows computed + 详情页 HTML |
| 修改单号规则 | 所有引用该单号的页面 |

### 7.3 页面间跳转

所有页面通过侧边栏统一导航，路由 key：

| key | 页面 | 路径 |
|---|---|---|
| stock | 备货计划 | pages/stock-plan/index.html |
| purchase | 采购计划 | pages/purchase-plan/index.html |
| shipment | 发货计划 | pages/shipment-plan/index.html |
| purchaseOrder | 采购单 | pages/purchase-orders/index.html |
| shipmentOrder | 发货单 | pages/shipment-orders/index.html |
| skuFirstLegCost | SKU 单位头程成本 | pages/sku-first-leg-cost/index.html，位于发货单下方 |

---

## 九、跨模块共享数据

### 8.1 枚举值（各计划页高度一致）

| 维度 | 示例值 |
|---|---|
| 平台 | Amazon, eBay, B2C, Shopify, Walmart |
| 仓库 | 深圳仓, 东莞工厂, 义乌仓, 洛杉矶海外仓 |
| 团队 | 亚马逊团队, Jasic团队, eBay团队, B端团队, 北美一组/二组, 欧洲组 |
| 创建人 | Admin, 张敏, 李晨, 王磊 |
| 国家 | 美国, 英国, 德国, 日本, 中国 |
| 头程服务商 | 马士基, DHL, 顺丰国际, 递四方, 万邑通 |
| 运输方式 | 海运, 空运, 快递 |

### 8.2 测试数据量

| 页面 | 数据条数 |
|---|---|
| 备货计划 | 32 条 |
| 采购计划 | 62 条 |
| 发货计划 | 62 条 |
| 采购单 | 27 条 |
| 发货单 | 33 条 |

---

## 十、设计决策记录

### 9.1 为什么取消「部分提货」状态？
- **原设计**：提货弹窗中如果部分 SKU 有差异，发货单状态可能为「部分提货」
- **当前设计**：提货差异通过「差异原因」字段记录，发货单状态直接跳到「待提货」（还有剩余）或「可加工/待发货」（全部提完），不含独立的「部分提货」态
- **原因**：减少状态数量，降低用户认知成本；差异在提货明细中可追溯

### 9.2 为什么提货后区分「可加工」和「待发货」？
- **含组合 SKU 的发货单**：提货完成后先进入「可加工」，因为组合品需要在物流环节拆解加工
- **纯普通 SKU 的发货单**：提货完成后直接进入「待发货」
- **原因**：组合品需要额外的加工工序，不能用同一个状态

### 9.3 为什么加工弹窗不显示非组合 SKU？
- 普通 SKU 不需要加工，出现在弹窗中只会干扰用户
- 加工弹窗聚焦于组合 SKU 的拆解和子件库存扣减

### 9.4 为什么加工弹窗合并库存相关字段？
- 与提货弹窗保持一致的用户体验
- 减少横向滚动，四行展示（在库量/待发量/在途量/总量）比四列更紧凑

### 9.5 为什么提货弹窗固定 SKU 和产品信息列？
- 用户横向滚动时仍需看到当前操作的是哪个 SKU
- 减少定位错误
